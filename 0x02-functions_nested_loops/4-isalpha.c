# include "main.h"
/**
 * _isalpah - function to check if character is lowercase or uppercase
 * @c - int to be used for the argument of the function
 * Return: 0
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
		Return (1);
	}
	else
		Return (0);
}
